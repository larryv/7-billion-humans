-- 7 Billion Humans (2215) --
-- 11: Injection Sites 1 --

-- Instructions: "Fill the data gaps in each row. Order does not matter."

-- Available commands: step, pickUp, drop, if, jump, comment

-- Size: 5 commands (challenge 5, shortest known 5)
-- Best average runtime: 10 seconds (challenge 7, fastest known 5)


pickup s
a:
step s
if w != datacube or
 c == datacube:
	jump a
endif
drop


-- vim: set autoindent noexpandtab shiftwidth=8:
